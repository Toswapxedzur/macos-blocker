# macosBlocker — Mac Vault (native macOS app)

> 🤖 **AI protocol:** Read `../package-info.md` (group), the group `AGENTS.md`, and `../misc/project-memory/PROJECT-MEMORY.md` before working here. Update this file when the folder changes. Never delete without owner consent; keep secrets out of git.

- **What:** the native macOS member of the suite (public name **Mac Vault**): Swift policy engine, WebView editor (mirrored extension UI in `Sources/MacBlockerWebUI/WebAssets`), app inventory + enforcement adapters, and the **local Vault hub host** (dev `ws://127.0.0.1:18787`, prod 8787) plus an **MCP HTTP server for agents** (dev port 18788 = the `vault-dev` MCP entry in Claude's config; only reachable while the app runs).
- **Own git repo:** `Toswapxedzur/macos-blocker`, branch `main`; releases `v0.0.1`, `v0.0.2` (+ `release/` branches). Version source of truth = `XcodeProject/project.yml` (`MARKETING_VERSION` / `CURRENT_PROJECT_VERSION`).
- **Build/test:** `swift build`, `swift test` (SwiftPM, `Package.swift`); dev launch `run-mac-vault.sh` (signs with a Developer ID identity). Release DMG pipeline in `scripts/release/` (see `RELEASE.md`).
- **Folders:** `Sources/` (`MacBlockerCore`, `MacBlockerAppFeature`, `MacBlockerWebUI`, `MacBlockerMacControl`, `MacBlockerPanel`, `MacBlockerScreenTime`), `Tests/`, `XcodeProject/` + `XcodeScaffold/` (app + Safari-extension targets, `SafariWebExtensionHandler.swift`), `Assets/Branding/` (official Mac icon sources), `i18n-docs/`, `LEGAL/`, `dist/` + `release/` (artifacts), `tools/`.
- **Mirror rule:** `Sources/MacBlockerCore/Resources/{helpers,event-sandbox}.js` must stay byte-identical to customBlocker — refresh with `../safariBlocker/sync-engine.sh`. The WebAssets popup mirror is hand-maintained and currently parked (owner decision 2026-08-07).
